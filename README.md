# Programming
## Лабораторная работа №1. Создание репозитория
1. Установка системного версионного контроля git;
2. Регистрация на сервисе размещения репозиториев онлайн GitHub;
3. Создание репозитория "Programming";
4. Добавление новой ветки develop и настраиваем ее ветвью по умолчанию;
5. Клонирование репозитория на локальную машину;
6. Создание текстового файла в репозитории и написания в нем произвольного текста;
7. Фиксация изменений в локальном репозитории и синхронизация с удаленным репозиторием;
8. Открытие текстового файла через GitHub и добавление нового текста, фиксация изменений и синхронизация с локальным репозиторием.
---
## Лабораторная работа №2. Перечисления
1. Создание проекта Programming в локальном репозитории. Тип проекта Windows Forms Application;
2. Создание двух подпапок в проекте Model и View. Model хранит бизнес-логику, View - формы и сопроводительные классы;
3. Переименование Form1 в MainForm и перемещение в папку View;
4. Добавление в папку Model перечисления дней недели, жанров кино, цветов, форм обучения студента, производителей смартфонов и времен года;
5. На главном окне формы размещаем TabControl с одной вкладкой Enums, выбираем в свойствах элемента Dock и выставляем значение Fill;
6. Для соответствия приложения с макетом добавляем GroupBox, в него помещаем TextLabel (лейблы полей), ListBox (списки), TextBox (поле для ввода);
7. В первый список добавляем перечень всех перечислений;
8. Добавляем в поле ввода вывод индекса выбранного варианта перечисления;
9. Добавляем парсинг дней недели;
10. С использованием интерполяции строк выводим название дня недели и его индекс (т.к. отсчет идет с понедельника, то  прибавляем 6 к индексу, делим на 7 и прибавляем в конце 1, чтобы воскресенье было завершающим днем), если введенные данные не соответствуют названию одного из дней недели, то под текстовым полем ввода появляется сообщение, что такого дня недели нет, а если значение введенных данных пустое, то выводим сообщение о вводе данных;
11. Добавляем интерактивный выбор времени года при помощи switch-кейсов. В выпадающем списке при выборе зимы/лета выводим сообщение, при выборе весны/осени - меняем цвет фона.
---
## Лабораторная работа №3. Классы
1. Описанные в задании классы переводим с формального языка на язык С#;
2. Делаем все поля классов строго закрытыми;
3. Для каждого поля в каждом классе создаем открытые свойства - методы-аксессоры, предоставляющие доступ к полям объектов в клиентском коде;
4. Добавляем проверку вводимых данных в полях классов, где это требуется, если не имеет ограничений - создаем автосвойство;
5. В каждый созданный класс добавляем конструкторы, инициализирующий все поля класса;
6. Добавляем в каждый класс конструктор без аргументов для возможности создания объектов без инициализации полей;
7. В главном окне добавляем новую вкладку Classes в элемент Tab-Control, элементы согласно макету задания;
8. В логике главного  окна добавляем закрытое поле, хранящее массив прямоугольников;
9. В конструкторе главного окна инициализируем массив прямоугольников, состоящий из 5 объектов, значения длины и ширины определяются с помощью генератора рандомных чисел;
10. Добавляем обработчик события, который при смене выбранного элемента меняет объект на текущий из объектов массива прямоугольников по текущему индексу;
11. При смене выбранного элемента поля в списке прямоугольников автоматически заполняются данными в соответствующих полях;
12. Для каждого поля добавляем обработчик события, работа которого заключается в том, что пользователь может вручную задавать параметры текущего выбранного прямоугольника;
13. Добавляем обработчик ошибок, который проверяет формат данных и допустимый диапазон, методом исключений при помощи блока try. При выявлении ошибки фон подсвечивается красным цветом;
14. Добавляем закрытый метод для нахождения прямоугольника, имеющего максимальную ширину. Метод принимает на вход массив прямоугольников, перебирает объекты и находит индекс соответствующего условию прямоугольника;
15. Добавляем кнопку, через обработчик события (при нажатии на кнопку) вызывается метод и возвращает индекс, заполяя поля данными найденного объекта;
16. То же самое проделываем с классом фильм, кнопка реализует метод поиска фильма с наибольшим рейтингом.
---
## Лабораторная работа №4. Приемы разработки классов
### Закрытые методы класса:
1. В ранее созданный класс контакта добавляем свойства имени и фамилии;
2. Добавляем проверку, что поля должны содержать только символы английского языка;
3. Реализуем проверку в виде закрытого метода, который будет вызываться в обоих свойствах этого класса;
4. Реализуем вывод ошибки с указанием поля, в котором выбрасывается исключение.
### Сервисные классы:
1. Создаем статический класс Validator, добавляем в него открытый статический метод, который выбрасывает исключение, если входное значение не является положительным, текст сообщения указывает на имя свойства, в котором было обнаружено исключение;
2. Заменяем все проверки на неотрицательные значения в ранее реализованных классах на вызовы класса Validator;
3. Реализуем перегрузку метода для вещественных полей и так же заменяем их в реализованных классах вызовом Validator;
4. Реализуем метод проверки, проверяющий значение, входящее в диапазон между двумя числами;
5. Заменяем в исходных классах все проверки на вызов методов из класса Validator.
### Поля, доступные только для чтения:
1. Создааем класс точек в двумерном пространстве, хранящие координаты х и у. Поля реализуем только для чтения, а значения генерируются только через конструктор и не меняются;
2. Поля должны проходить проверки на неотрицательность координат и превышение диапазона. Поля должны содержать сеттеры и быть приватными, конструктор класса будет инициализировать поля через них;
3. В класс прямоугольников добавляем свойство Center и инициализацию точки в конструкторе;
4. Модифицируем пользвовательский интерфейс программы с отображением значений координат центра прямоугольника в текстовых полях, доступных только для чтения;
### Зависимые и взаимозависимые поля:
1. Создаем класс Ring, описывающий геометрическое кольцо. Класс должен хранить в себе центр координат и значения внешнего и внутреннего радиусов. Создаем валидацию, чтобы внутренний радиус не превышал внешнего, а внешний не был меньше внутреннего, а в случае получения некорректных данных сеттер выбрасывает исключение. Реализуем конструктор класса;
2. Добавляем в класс доступное только на чтение свойство, возвращающее площаль кольца;
3. Свойство нахождения площади кольца должно рассчитываться заново каждый раз, когда это свойство вызывается в клиентском коде;
### Статические поля и методы:
1. В классе прямоугольников создаем закрытое статическое целочисленное поле, предназначенное для подсчета всех существующих прямоугольников;
2. Там же создаем открытое статическое свойство, возвращающее значение статического поля из прошлого пункта;
3. Добавляем в конструктор класса прямоугольника строку, увеличивающую значение статического поля подсчета прямоугольников на единицу, при каждом вызове конструктора происходит увеличение счетчика;
4. В класс прямоугольников добавляем поле и свойство id, доступное только для чтения. Поле должно инициализироваться в конструкторе класса значением из прошлого пункта;
5. Добавляем в пользовательский интерфейс вывод свойства id для прямоугольника, текстовое поля должно быть доступно только для чтения;
6. Создаем статический класс, выполняющий проверку о пересечении/столкновении геометрических фигур. Класс реализует два метода: для прямоугольников, принимающий два экземпляра на вход и проверяющий их пересечение, для колец, принимающий два экземпляра на вход и проверяющий их пересечение. Данные методы являются булевыми, возвращающие либо 1, либо 0.
---
## Лабораторная работа №5. Коллекции
1. Создаем новую вкладку для прямоугольников согласно макету задания;
2. Даем всем элементам новой вкладки правильные названия, согласно требованиям к именованиям;
3. Не трогаем ранее созданные вкладки других классов;
4. Создаем в главном окне поля _rectangles и _currentRectangle типа List<Rectangle> и Rectangle соответственно;
5. Реализуем кнопку и логику кнопки "Добавить прямоугольник". В обработчике кнопки создается новый экземпляр прямоугольника, который будет добавляться в список _rectangles;
6. Все прямоугольники списка должны отображаться в списке ListBox на вкладке с соблюдением формата вывода строки согласно макету;
7. Реализуем кнопку и логику кнопки "Удалить прямоугольник". В обработчике кнопки определяется текущий выбранный в списке прямоугольник и удаляется из обоих списков. Если ничего не выбрано или список пуст, то при нажатии на кнопку ничего не происходит;
8. При выборе прямоугольника в списке, все его данные отображаются в соответствующих текстовых полях. Если ничего не выбрано или список пуст, то текстовые поля очищаются;
9. Реализуем возможночть редактирования данных прямоугольника с последующим сохранением и обновлением данных;
10. Все текстовые поля реализовывают валидацию согласно ограничениям на поля в классах;
11. Поле id остается доступным только для чтения;
12. Реализуем отображение прямоугольников на канве при помощи добавления новых экземпляров на панель канвы;
13. Создаем в главном окне еще одно поле _rectanglePanels типа List<Panel>;
14. В обработчик кнопки "Добавить прямоугольник" добавляем код, который создает переменную типа Panel. Свойства размеров и положения созданной панели инициализируются со значениями прямоугольника, который создается в этом же обработчике. Назначаем цвет фона панели зеленым;
15. Добавляем код для отрисовки панелей на канве;
16. Реализуем логику удаления панелей с канвы при удалении прямоугольников из списка;
17. Реализуем логику индикации прямоугольников, которые пересекаются изменением их фона на красный цвет;
18. Для индикации пересечений создаем отдельный закрытый метод в главном окне, логика которого заключается в перекрашивании всез панелей в зеленый, перебор всех прямоугольников в двойном цикле, на каждой итерации которого сраниваются i-ый и j-ый прямоугольники, при нахождении пересечения эти прямоугольники перекрашиваются в красный;
19. Данный метод должен вызываться каждый раз, когда происходит добавление/редактирование/удаление прямоугольника;
20. Панель на канве меняется только, если пользователь ввел корректные значения для полей прямоугольника;
21. Реализуем методы обновления данных в текстовых полях по указанному прямоугольнику, очищению всех текстовых полей со значениями прямоугольника, статический класс, возвращающий новый случайный объект прямоугольника;
22. Помещаем созданные классы бизнес-логики в отдельную папку;
23. Меняем кнопки на PictureBox, добавляем им события при наведении и нажатии (т.к. иконки ч/б, то просто меняем прозрачность при событиях) и убираем внешний контур;
24. Добавляем отступ до краев канвы в 30 пикселей;
25. Так как цикл с нахождением пересечений неоптимален и замедляет работу, создаем сетку и ячейки для быстрого и оптимального нахождения пересечений, таким образом не вызывается перерисовка всех панелей каждый раз при изменении данных.
---
## Лабораторная работа №6. Пользовательские элементы управления
1. Создаем в проекте новый элемент пользовательского управления для прошлого задания, переносим все события, методы и логику;
2. То же самое проделываем и с остальными вкладками классов;
3. Переносим всю логику на окно формы, удаляя старую;
4. Создаем статический класс и выносим туда все цвета для валидации, пересечения прямоугольников, смены времен года и т.д.
---
## Лабораторная работа №7. Автодокументирование кода
1. Добавляем xml-комментарии для всего исходного кода, который мы написали в рамках предудищих заданий.
---
## Лабораторная работа №8. Разработка информационной системы
1. Реализуем программу в отдельном решении среды разработки, но в том же репозитории;
2. Информационная система согласно заданному варианту:
* Список книг:
Программа предоставляет доступ к перечню книг. О каждой книге про-
грамма хранит:
- название книги (не более 100 символов);
- год выпуска (не более текущего года);
- автор;
- количество страниц (строго положительное значение);
- жанр (перечисление, в пользовательском интерфейсе выбирается с
помощью элемента ComboBox).
В левой колонке (элемент ListBox) отображаются книги в формате
«Название / Автор / Жанр», все книги должны быть отсортированы в
алфавитном порядке;
3. Создаем методы и классы, реализуем логику работы приложения и верстку, прежде чем вынести это в пользовательский элемент управления;
4. Реализуем функции просмотра всех созданных в программе данных в виде общего списка и панели подробной информации, создание/редактирование/удаление данных элементов списка, сортировка данных в алфавитном порядке вызывается каждый раз при взаимодействии с элементами списка, при редактировании данных после сохранения они сразу же отображаются в списке, валидация данных, всплывающие подсказки, сохранение данных при закрытии в файл json, при открытии загружает данные из этого файла и показывает пользователю;
5. Добавляем к исходному коду xml-комментарии;
6. Собираем программу в установочный пакет. Для этого загружаем расширение Microsoft Visual Studio Installer Projects и создаем новый проект с этим же решением, в RemovePreviousVersions указываем true, в Application Folder помещаем иконки приложения, в основных выходных данных указываем проект. В Application Folder создаем путь для приложения, в Working Folder должна быть указана папка Application Folder, путь переносим в папку User's Desktop, добавляем иконку и имя. Теперь после установки приложение будет появляться на рабочем столе пользователя. Создаем новую папку, выбирая Windows, туда добавляем файл msiexec.exe, который находится в C:\Windows\System32\msiexec.exe, это и есть файл деинсталляции нашего приложения. Создаем путь до деинсталлятора, в поле target должно быть прописано имя файла деинсталлятора, добавляем иконку и имя, в аргументах прописываем "/x {код-продукта}" и переносим в папку User's Programs Menu. Установщик и деинсталлятор готовы, собираем приложение.